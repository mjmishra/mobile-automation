<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender: coloured output in terminal -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender: persists logs per run -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>reports/automation.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>reports/automation.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress verbose Appium / Selenium wire logs -->
    <logger name="io.appium"           level="WARN"/>
    <logger name="org.openqa.selenium" level="WARN"/>
    <logger name="io.netty"            level="WARN"/>

    <!-- Our framework: INFO and above -->
    <logger name="com.saucelabs" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
